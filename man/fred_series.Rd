% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tips-data.R
\name{fred_series}
\alias{fred_series}
\title{Download a series from FRED}
\usage{
fred_series(
  series_id,
  at_dates = NULL,
  url = "https://fred.stlouisfed.org/graph/fredgraph.csv?id=\%s",
  quiet = TRUE
)
}
\arguments{
\item{series_id}{A FRED series identifier, e.g. \code{"CPIAUCNS"}.}

\item{at_dates}{Optional vector of dates. When supplied, one row is
returned per date, carrying the last observation at or before it forward.}

\item{url}{Template for the download, with \verb{\%s} for the series id.}

\item{quiet}{Suppress the download progress bar.}
}
\value{
A data frame with \code{date} and \code{value}.
}
\description{
A small convenience for the two macro inputs \code{\link[=atsm_real]{atsm_real()}} needs that are
not yield curves: a price index and a short rate.
}
\section{Which series}{

The paper uses seasonally \strong{unadjusted} CPI-U as the price index the TIPS
payouts are tied to (\code{"CPIAUCNS"}), and the effective federal funds rate as
the nominal short rate (\code{"DFF"} daily, or \code{"FEDFUNDS"} monthly).

Pass \code{at_dates} for a short rate. \code{r_t} in the model is the riskless return
earned from \code{t} to \code{t + 1}, so what is wanted is the rate \emph{prevailing on}
each panel date, not a backward-looking average of the month that just
ended. With \code{at_dates} the most recent observation at or before each date
is returned, which also sidesteps the fact that a month-end panel date can
be a weekend with no FRED observation on it.

A price index needs no such alignment: \code{\link[=atsm_real]{atsm_real()}} matches it by calendar
month, because an index dated to the first of the month and a curve dated
to the last business day would never join exactly.
}

\section{Requirements}{

A working internet connection. FRED is not a dependency of the package and
nothing here is redistributed.
}

\examples{
\dontrun{
cpi <- fred_series("CPIAUCNS")
ff <- fred_series("DFF", at_dates = unique(gsw_monthly$date))
}

}
\seealso{
\code{\link[=atsm_real]{atsm_real()}}, \code{\link[=gsw_tips]{gsw_tips()}}
}
