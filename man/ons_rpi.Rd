% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boe-data.R
\name{ons_rpi}
\alias{ons_rpi}
\title{Download the UK retail price index}
\usage{
ons_rpi(extend_to = NULL, quiet = TRUE, check = TRUE)
}
\arguments{
\item{extend_to}{Earliest date the index is required to cover. Backward
chaining runs until it is reached. \code{NULL} returns the published index
only.}

\item{quiet}{Suppress download progress bars.}

\item{check}{Verify the chaining relation against the published index.}
}
\value{
A data frame with \code{date} (first of the month), \code{value} (index) and
\code{chained}, flagging months reconstructed rather than published.
}
\description{
Fetches the Office for National Statistics' RPI All Items index, which is
the price series UK index-linked gilts are tied to and the one the
Supplementary Appendix of Abrahams et al. (2016) uses as \eqn{Q_t}.
}
\section{Why two series are downloaded}{

The published index (\code{CHAW}) begins in \strong{January 1987}, because that is
when it was rebased to 100, but the UK specification starts in January
1985 and its first monthly inflation reading needs December 1984. The
twelve-month percentage change (\code{CZBH}) runs from 1948, so the index is
chained backwards through it:
\deqn{RPI_t = RPI_{t+12} / (1 + CZBH_{t+12}/100)}

The chaining is verified, not assumed. The same relation applied \emph{forwards}
from 1987 predicts the published index for later years, and that
reconstruction is checked against the real thing before any extension is
returned.
}

\section{How accurate the extension is}{

\code{CZBH} is published to one decimal place, so each chaining step carries
about half a basis point of a percent of rounding error in the level. That
is harmless for the \emph{mean} of inflation but not negligible in a single
month's change: two adjacent chained months round independently, which puts
roughly 80 basis points of annualised noise on the monthly inflation
reading. Reconstructed months are flagged in the \code{chained} column so the
sensitivity can be checked -- 25 of the 336 months of the UK sample are
affected, and \code{analysis/validate-uk.R} reports the fit with and without
them.
}

\examples{
\dontrun{
rpi <- ons_rpi(extend_to = "1984-12-01")
sum(rpi$chained)
}

}
\seealso{
\code{\link[=atsm_real]{atsm_real()}}, \code{\link[=boe_yield_curve]{boe_yield_curve()}}
}
