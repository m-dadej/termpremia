% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forwards.R
\name{forward_curve}
\alias{forward_curve}
\title{The one-month forward curve on a single date}
\usage{
forward_curve(object, date = NULL, ...)
}
\arguments{
\item{object}{An \code{atsm_fit} or \code{atsm_real_fit}.}

\item{date}{A date in the estimation sample. Defaults to the last one.}

\item{...}{Unused.}
}
\value{
A data frame with \code{maturity}, \code{component} and \code{value}. Maturities
run from the second fitted month, since a one-month forward needs a
predecessor.
}
\description{
The cross-section that \code{\link[=forward_rate]{forward_rate()}} takes a slice of: the rate locked
in for each single month ahead, from the observed curve and from the
model's fitted and risk-neutral curves.
}
\section{Reading it}{

The gap between the fitted and risk-neutral forward curves is the forward
term premium by maturity. Where they run together, the model is saying the
curve at that horizon is expectations; where they separate, it is premium.
The observed series is there to show the fit, and differs from \code{fitted} by
the pricing error alone.
}

\examples{
panel <- yield_panel(gsw_monthly, units = "percent",
                     maturity_unit = "months", issuer = "US")
fit <- atsm(panel, n_factors = 5)
fc <- forward_curve(fit)
head(fc)

}
\seealso{
\code{\link[=forward_rate]{forward_rate()}}
}
