% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tips-data.R
\name{gsw_tips}
\alias{gsw_tips}
\title{Download the Gurkaynak-Sack-Wright TIPS zero-coupon curve}
\usage{
gsw_tips(
  maturities = 23:120,
  align_to = NULL,
  end = NULL,
  url = paste0("https://www.federalreserve.gov/data/",
    "yield-curve-tables/feds200805.csv"),
  cache = NULL,
  quiet = TRUE,
  check = TRUE
)
}
\arguments{
\item{maturities}{Maturity grid in months. The default starts at 23 rather
than 24 so that a one-month return on a two-year TIPS can be formed;
see \code{\link[=atsm_real]{atsm_real()}}.}

\item{align_to}{Optional vector of dates, normally the nominal curve's
month-ends, to relabel observations onto. See below.}

\item{end}{Optional last date to keep.}

\item{url}{Source location, exposed so a local copy can be used.}

\item{cache}{Optional path to a downloaded copy. When it exists it is read
instead of downloading; when it does not, the download is saved there.}

\item{quiet}{Suppress the download progress bar.}

\item{check}{Verify the evaluated curve against the file's own published
\code{TIPSY} columns and stop if they disagree by more than a tenth of a basis
point. Verifying against tenors that are off the grid would compare
nothing and pass, so off-grid check tenors are an error.}
}
\value{
A data frame with \code{date}, \code{maturity} (months), \code{yield} (percent)
and \code{extrapolated}, flagging maturities beyond the longest tenor the file
itself publishes on that date.
}
\description{
Fetches the Federal Reserve Board's fitted real yield curve (series
\code{feds200805}) and evaluates it onto a monthly maturity grid, which is the
form \code{\link[=atsm_real]{atsm_real()}} needs.
}
\section{Why the published columns cannot be used directly}{

The same three traps as the nominal file, plus two that are specific to
this one.

The published \code{TIPSY} columns run \code{TIPSY02} to \code{TIPSY20} in \strong{annual}
steps from two years. A one-month holding return needs maturities \code{n} and
\code{n - 1} adjacent on the grid, so the curve has to be re-evaluated from the
Svensson parameters rather than read off.

Rows switch between functional forms, and the encoding is \strong{not} the
nominal file's \code{-999.99} sentinel: here a Nelson-Siegel row carries a
literal \code{NA} in \code{BETA3} and \code{TAU2}. All of 1999-2003 is Nelson-Siegel,
because too few TIPS were outstanding to identify the second hump, and
scattered later rows are too. A missing \code{TAU2} therefore does not mean a
missing curve, and code that treats it that way discards five years of
data.

Separately, rows with \strong{no} curve at all -- every parameter \code{NA} -- fall
on US federal holidays. Nine of them are the last calendar day of their
month, concentrated in March and May (Good Friday and Memorial Day), so
selecting month-ends before dropping them silently loses those months.
This is the same order-of-operations bug that cost twenty months on the
nominal curve, and the filtering here is deliberately in the safe order.
}

\section{Joining to a nominal curve}{

Each file's month-end is whichever business day that month last carried a
fitted curve, and the two files do not always agree: over 1999-2024 the
nominal and TIPS curves share 311 of 312 calendar months exactly, but in
June 2003 the nominal file has a curve on the 30th and the TIPS file's last
one is the 27th. A \link{yield_panel} keys on exact dates, so that single
mismatch is enough to put a one-month hole in the joint panel -- which
\code{\link[=atsm_real]{atsm_real()}} then refuses to fit, correctly, because its factor VAR reads
consecutive rows as consecutive months.

Pass \code{align_to} with the nominal curve's dates to avoid this. Each TIPS
observation is relabelled to the nominal date for the same calendar month,
which is what a monthly panel means in any case; months absent from
\code{align_to} are dropped, and the count is reported.
}

\examples{
\dontrun{
tips <- gsw_tips(cache = "feds200805.csv")
str(tips)
}

}
\references{
Gurkaynak, R. S., B. Sack and J. H. Wright (2010). "The TIPS yield curve
and inflation compensation." \emph{American Economic Journal: Macroeconomics}
2(1), 70-92.
}
\seealso{
\code{\link[=atsm_real]{atsm_real()}}, \code{\link[=fred_series]{fred_series()}}
}
