% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.atsm_fit}
\alias{plot.atsm_fit}
\title{Plot a fitted term structure decomposition}
\usage{
\method{plot}{atsm_fit}(
  x,
  maturity = NULL,
  type = c("decomposition", "premium", "residuals"),
  col = c("grey25", "steelblue", "#E8A0A0"),
  ...
)
}
\arguments{
\item{x}{An \code{atsm_fit}.}

\item{maturity}{Maturity in months. Defaults to the longest fitted.}

\item{type}{One of:
\describe{
\item{\code{"decomposition"}}{The observed yield and the risk-neutral
(expected average short rate) component, with the term premium shaded
between them. The canonical chart: the premium \emph{is} the gap.}
\item{\code{"premium"}}{The term premium alone, with a zero line. Negative
premia are common post-2010 and worth being able to see.}
\item{\code{"residuals"}}{Pricing errors over time and fit quality by
maturity, for checking that the model actually fits the curve.}
}}

\item{col}{Length-3 vector of colours for the yield, risk-neutral component
and premium shading.}

\item{...}{Passed to the underlying plotting calls.}
}
\value{
\code{x}, invisibly. Called for the side effect.
}
\description{
Base graphics, so the package carries no plotting dependency.
}
\examples{
panel <- yield_panel(gsw_monthly, units = "percent",
                     maturity_unit = "months", issuer = "US")
fit <- atsm(panel, n_factors = 5)

plot(fit)                       # 10y decomposition
plot(fit, type = "premium")
plot(fit, maturity = 24, type = "decomposition")

}
