% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/real-nominal-ml.R
\name{acmy_control}
\alias{acmy_control}
\title{Settings for the maximum likelihood step}
\usage{
acmy_control(
  max_outer = 20L,
  max_inner = 400L,
  penalty = 1,
  penalty_factor = 10,
  tol = 1e-06,
  reltol = 1e-10,
  ndeps = 1e-06
)
}
\arguments{
\item{max_outer}{Maximum augmented-Lagrangian rounds.}

\item{max_inner}{Maximum \code{optim()} iterations within a round.}

\item{penalty}{Initial penalty on constraint violation.}

\item{penalty_factor}{Multiplier applied to the penalty in a round that
fails to reduce the violation materially.}

\item{tol}{Constraint violation below which the fit is taken as converged,
in units of a factor's standard deviation.}

\item{reltol}{Relative convergence tolerance passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{ndeps}{Step used for the numerically differenced gradient. The
\code{optim()} default of 1e-3 is an ABSOLUTE step, which is far too coarse
once the penalty term makes the merit surface steep: the gradient stops
being informative and the inner optimiser reports convergence after a
handful of evaluations without having moved.}
}
\value{
A list of validated settings.
}
\description{
Settings for the maximum likelihood step
}
\examples{
# Push harder on the constraints than the default.
acmy_control(tol = 1e-10, max_outer = 20)

}
\seealso{
\code{\link[=atsm_real]{atsm_real()}}
}
