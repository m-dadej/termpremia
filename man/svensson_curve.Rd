% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svensson.R
\name{svensson_curve}
\alias{svensson_curve}
\title{Evaluate a panel of Nelson-Siegel or Svensson curves}
\usage{
svensson_curve(params, maturity)
}
\arguments{
\item{params}{A data frame or matrix with one row per date and columns
\code{beta0}, \code{beta1}, \code{beta2}, \code{beta3}, \code{tau1}, \code{tau2}. Column names are
matched case-insensitively. Rows whose parameters are missing produce a row
of \code{NA}.}

\item{maturity}{Numeric vector of maturities, \strong{in years}.}
}
\value{
A numeric matrix with \code{nrow(params)} rows and \code{length(maturity)}
columns. Row names are taken from \code{params} if present; column names are the
maturities.
}
\description{
Vectorised counterpart to \code{\link[=svensson_yield]{svensson_yield()}}: evaluates one curve per row of
\code{params} on a shared maturity grid, which is how a published parameter file
such as Gurkaynak, Sack and Wright's is turned into a yield panel.
}
\examples{
params <- data.frame(
  beta0 = c(4, 4.1), beta1 = c(-2, -1.9), beta2 = c(1, 1.1),
  beta3 = c(0.5, 0.4), tau1 = c(1.5, 1.5), tau2 = c(10, 10)
)
svensson_curve(params, maturity = c(1, 5, 10))

}
