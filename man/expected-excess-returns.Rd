% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expected-returns.R
\name{expected-excess-returns}
\alias{expected-excess-returns}
\alias{expected_excess_return}
\alias{expected_excess_return.atsm_real_fit}
\alias{expected_excess_return_loadings}
\title{Expected one-period excess bond returns}
\usage{
expected_excess_return(object, maturity = NULL, convexity = FALSE, ...)

\method{expected_excess_return}{atsm_real_fit}(
  object,
  maturity = NULL,
  convexity = FALSE,
  curve = c("nominal", "real"),
  ...
)

expected_excess_return_loadings(object, maturity = NULL, ...)
}
\arguments{
\item{object}{An \code{atsm_fit} or \code{atsm_real_fit}.}

\item{maturity}{Maturities in months. Defaults to every fitted maturity.}

\item{convexity}{When \code{TRUE}, the returned frame carries the risk
compensation and the convexity adjustment as separate components as well
as their sum.}

\item{...}{Unused.}

\item{curve}{For a joint fit, \code{"nominal"} or \code{"real"}.}
}
\value{
\code{expected_excess_return()} returns a data frame with \code{date},
\code{maturity} and \code{value}, in monthly decimal units -- these are one-month
returns, not annualised rates, so multiplying by 12 would misstate them.
With \code{convexity = TRUE} there is also a \code{component} column.
\code{expected_excess_return_loadings()} returns a data frame with \code{maturity},
\code{factor} and \code{value}.
}
\description{
The return an \code{n}-month bond is expected to earn over the next month in
excess of the one-month rate, given the factors today.
}
\section{The formula}{

\deqn{E_t\left[rx^{(n-1)}_{t+1}\right] =
  B_{n-1}'(\lambda_0 + \lambda_1 X_t)
  - \tfrac12\left(B_{n-1}'\Sigma B_{n-1} + \sigma^2\right)}

The first term is compensation for risk: the bond's exposure to the factors
priced by the prices of risk. The second is a Jensen term, the convexity
adjustment that makes a log return differ from the log of an expected
return; \code{expected_excess_return()} can return it separately.

Note the loading is \eqn{B_{n-1}}, not \eqn{B_n}. A bond bought with \code{n}
months to run is sold a month later with \code{n - 1}, so it is the shorter
bond's sensitivity that determines the risk taken. A one-month bond held
one month is riskless and its expected excess return is exactly zero, which
the recursion delivers through \eqn{B_0 = 0} and which is asserted in the
tests.
}

\section{Loadings}{

\code{expected_excess_return_loadings()} gives \eqn{B_{n-1}'\lambda_1}, scaled
by each factor's standard deviation, so an entry reads as the effect on the
expected excess return of a one standard deviation move in that factor. It
is the compact answer to "which factor is driving the premium".
}

\examples{
panel <- yield_panel(gsw_monthly, units = "percent",
                     maturity_unit = "months", issuer = "US")
fit <- atsm(panel, n_factors = 5)

er <- expected_excess_return(fit, maturity = 120)
mean(er$value) * 1e4        # basis points per month

expected_excess_return_loadings(fit, maturity = c(24, 120))

}
\seealso{
\code{\link[=term_premium]{term_premium()}}, \code{\link[=forward_rate]{forward_rate()}}
}
